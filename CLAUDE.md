# 試菜與標準食譜系統（暫名）

麵店籌備團隊的內部網站，介面只用繁體中文。用途是把菜品、湯底、醬料、配料和製作流程都標準化、量化，而且查得到來源；試菜期間能快速修改食譜版本，定版後能印出內場直接使用的標準卡，並自動算出成本與毛利。

完整規格在 [docs/SPEC.md](docs/SPEC.md)。本檔是開發時一定要遵守的規則。如果本檔和 SPEC 有衝突，以本檔為準，並告訴使用者要改哪一份。

## 目前階段

**MVP 程式已完成第一版，等 Supabase 設定好就會部署。**（2026-09-17）

- 已完成：資料庫（migrations、RPC、凍結與稽核 trigger）、計算核心、所有 MVP 頁面、示範模式、基準版菜單匯入腳本
- repo：**公開**的 https://github.com/greedydavis/recipe-system （使用者選擇公開原始碼）。推送 `main` 會觸發 `.github/workflows/deploy.yml`：先跑測試；repo 變數 `VITE_SUPABASE_URL`、`VITE_SUPABASE_ANON_KEY` 都設定後，才會部署到 https://greedydavis.github.io/recipe-system/
- 公開前把歷史壓成單一 commit；清理前的舊歷史只保留在本機標籤 `local/history-before-public`，**不要推送 tags**
- 尚未完成：建立 Supabase 正式專案與創辦人帳號（使用者操作）、設定 repo 變數、Playwright E2E、真實單價與克重回填、UAT
- 常用指令：`npm run dev`（本機，自動使用示範資料庫）、`npm test`、`npm run typecheck`、`npm run build`、`npm run db:bundle`、`npm run seed:baseline`

---

## 1. 範圍

### MVP 必做

- 菜品與元件（湯底、醬料、配料、麵條、其他）可以新增、編輯、搜尋
- 食譜版本控制：已定版的版本不能直接覆蓋
- 試菜紀錄與照片上傳
- 自動計算原物料成本、單位換算、損耗率、每份成本、食材毛利率和建議售價
- 份量換算：一份、十份、一批次，也可以自訂份數或產量
- 標準卡可以直接在手機上看，也可以用 A4／A5 列印
- 以手機為主的繁體中文介面
- 角色：創辦人、研發／主廚、店長、測試人員
- 所有重要修改都要留下操作紀錄

### MVP 不做

POS 串接；向供應商下單與採購流程；排班、薪資、會計；外送平台整合。
另外也不做：庫存與盤點、人工／瓦斯水電／租金分攤、內用與外帶兩套成本、營養與過敏原計算、多門市、離線編輯、多層簽核、Excel 或 OCR 匯入、原生 App、多語系。

需求如果落在「不做」清單裡，先提醒使用者，不要自己實作。

---

## 2. 角色與權限

| 角色 | 代碼 | 重點權限 |
|---|---|---|
| 創辦人 | `founder` | 全部權限；唯一能**核准定版**、停用已定版版本、設定售價、指派角色、修改系統設定的角色 |
| 研發／主廚 | `chef` | 建立和編輯食譜與草案、送試菜、送核准、維護原物料與價格、主持試菜 |
| 店長 | `manager` | 看完整食譜與成本（唯讀）、維護原物料與價格、建立試菜紀錄、評分、列印標準卡 |
| 測試人員 | `tester` | 只能看到被指派的試做項目（名稱或盲測代號、照片），可以填寫與修改**自己的**評分 |
| 待審核 | `pending` | 剛註冊的帳號，沒有任何權限，要由創辦人指派角色 |

- **權限一定要在資料庫層強制執行**。資料表都放在不對外公開的 `app` schema（啟用 RLS 且沒有任何 policy，前端角色也沒有權限）；前端**只能**呼叫 `public` schema 的 security definer RPC，每個 RPC 開頭用 `app.require_role(...)` 驗證角色。前端隱藏按鈕只是介面上的體驗，不算權限控管。
- 測試人員不管透過什麼 API，都不能讀到用料數量、步驟、單價、成本、供應商，也不能看到其他人的評分。
- 標準卡預設不顯示成本；只有創辦人可以印「含成本版」。

---

## 3. 資料定義

### 名詞

| 名詞 | 定義 |
|---|---|
| 菜品 `dish` | 賣給客人的品項。有售價。一個版本代表 **1 份**。 |
| 元件 `component` | 半成品：湯底 `soup`、醬料 `sauce`、配料 `topping`、麵條 `noodle`、其他 `other`。有批次產量和每份用量，可以被菜品或其他元件引用。 |
| 原物料 `ingredient` | 採購進來的東西，包含水和包材。每個原物料只有一種**基本單位**：`g`、`ml` 或 `pc`（個）。 |
| 包裝規格 `packaging_spec` | 採購單位，例如「20 kg/箱」「1 台斤」。每個原物料只能有一個**預設規格**。 |
| 單價 `purchase_price` | 某個包裝規格從某個生效日開始的採購金額。只會新增，不會覆寫；打錯時標記作廢再新增一筆。 |
| 單位換算 | 全域換算：kg=1000 g、台斤=600 g、兩=37.5 g、L=1000 ml、大匙=15 ml、小匙=5 ml。原物料專屬換算：例如 1 顆蛋=60 g。質量和體積互換時需要密度 g/ml。 |
| 損耗率 | 採購量在處理（去皮、修清、去骨）時損失的比例，範圍 0 ≤ r < 1。原物料有預設值，每一行用料可以另外覆寫。 |
| 淨重 | **食譜上填的用量一律是處理後、烹煮前的淨重。** 草案的用量可以留空（「用量待填」），但送試菜或送核准前必須填齊。 |
| 菜單分類 `menu_category` | 菜品的分類文字，沿用基準版菜單的分類（例如湯麵、拌麵、小菜、飲品）。 |
| 批次產量 | 一個批次實際產出的量（g 或 ml）。菜品的批次產量等於每份克重。 |
| 每份量／每份克重 | 元件：每份的用量（例如每碗湯底 420 g）；菜品：一份成品的重量。 |
| 出成率 | 批次產出重量 ÷ 投入淨重合計。可以大於 100%（例如麵條吸水）。產出用 ml 記錄時必須填成品密度，否則不顯示出成率（不影響成本）。**水必須列成用料**，出成率才會正確。 |
| 食材成本率／食材毛利率 | 只算食材，不包含人工、能源、租金。畫面上一律寫「食材」兩個字，避免誤會。 |

### 計算規則（唯一實作位置：`src/domain/`）

```
單位成本(每基本單位) = 預設包裝規格的最新有效單價 ÷ 該規格換算成基本單位的數量
採購量               = 淨重(基本單位) ÷ (1 − 損耗率)
行成本               = 採購量 × 單位成本
批次成本             = Σ 行成本
元件每單位產出成本   = 批次成本 ÷ 批次產量
引用元件的行成本     = 用量 × 被引用元件版本的每單位產出成本
每份成本             = 元件：每單位產出成本 × 每份量；菜品：Σ 行成本
未稅售價             = 含稅售價 ÷ (1 + 營業稅率)
食材成本率           = 每份成本 ÷ 未稅售價
食材毛利率           = 1 − 食材成本率
建議售價(含稅)       = 每份成本 ÷ 目標食材成本率 × (1 + 營業稅率)，無條件進位到「進位單位」的倍數
份量換算倍率         = 目標產量 ÷ 批次產量（菜品：目標份數 ÷ 1）
```

- **有效單價**：生效日 ≤ 計算日期、而且沒有作廢的單價裡，生效日最新的一筆。未來才生效的價格不能拿來算今天的成本。
- **單價 0 元**算「有價格」（例如水）。**沒有價格**或**無法換算單位**時，成本標示「不完整」，列出原因，也**不顯示**食材毛利率和建議售價。絕對不能把缺少的值當成 0，也不能自己猜換算比例。
- 金額與數量一律用 `decimal.js` 或 Postgres `numeric` 計算，禁止用 JS 浮點數直接相乘或相加。
- **計算過程中不四捨五入**，只在顯示時處理：
  - 用量：`g`、`ml` 小於 10 顯示到小數 1 位，10 以上顯示整數；計數單位顯示到小數 1 位
  - 單位成本顯示到小數 4 位；行成本 2 位；每份成本 1 位；批次成本顯示整數
  - 百分比顯示到小數 1 位；建議售價顯示整數
- 份量換算只縮放**用量**。時間、溫度、火力不跟著縮放，標準卡上要註明這一點。
- 系統設定預設值：目標食材成本率 35%、營業稅率 5%、建議售價進位到 5 元、送核准前需要有試菜紀錄（使用者已同意預設值）。

---

## 4. 版本控制原則

### 狀態

`draft` 草案 → `testing` 試菜中 → `pending_approval` 待核准 → `locked` 已定版 → `retired` 停用

| 從 → 到 | 誰可以 | 前置條件 | 必填 |
|---|---|---|---|
| 草案 → 試菜中 | 創辦人、主廚 | 至少 1 行用料；產量與每份量已填；引用的元件版本不是草案也不是停用 | — |
| 草案 → 待核准 | 創辦人、主廚 | 同上，而且成本完整 | 免試菜原因 |
| 試菜中 → 草案 | 創辦人、主廚 | 還沒有試做項目，也沒有被其他版本引用 | — |
| 試菜中 → 待核准 | 創辦人、主廚 | 至少 1 筆評分（依設定）；成本完整 | 送審說明 |
| 待核准 → 已定版 | **只有創辦人** | 引用的元件版本全部是已定版；成本完整 | 核准意見（可以留空） |
| 待核准 → 試菜中 | 只有創辦人 | — | 退回原因 |
| 試菜中／待核准 → 停用 | 創辦人、主廚 | — | 停用原因 |
| 已定版 → 停用 | 只有創辦人 | — | 停用原因 |
| 已定版 → 停用（自動） | 系統 | 同一個食譜的另一個版本定版時 | 自動填入「被 vN 取代」 |

### 規則

1. **只有草案可以修改內容。** 內容包括：用料、數量、單位、損耗覆寫、分組、產量、每份量、密度、步驟、時間、溫度、火力、關鍵管制點、保存方式、版本照片、步驟照片。
2. 版本一離開草案，內容就凍結。要修改只能用「**複製為新版本**」產生新的草案，並記錄 `based_on_version_id` 和修改說明。
3. 已定版的版本**不能修改、不能刪除**，只能改成停用。這條要由 **DB trigger** 強制執行，不能只靠前端或 RPC 檢查。
4. 每個食譜同一時間**最多只能有一個**已定版版本（partial unique index）。
5. 版本號在同一個食譜裡遞增，不回收；草案被刪除後，號碼也不會再用。
6. 草案可以刪除（會留下操作紀錄）；非草案不能刪除。
7. **引用元件時要綁定元件的特定版本**（`component_version_id`），而且只能引用已經凍結的版本（試菜中、待核准、已定版）。因為被引用的版本不會再改變，版本之間不會形成循環；另外還要禁止引用自己的食譜，並把巢狀深度限制在 5 層以內。
8. 元件出了新的定版後，引用舊版的菜品**不會自動更新**；首頁和標準卡會顯示「引用的元件版本已被取代」，要更新就得建立菜品的新版本。
9. 停用是終點狀態。要恢復，就複製成新版本，重新走一次流程。
10. 狀態只能透過 RPC `transition_version` 變更；每次變更都要寫入 `version_status_history`（操作者、時間、意見）。
11. 草案要用交易式 RPC `save_version_draft` 一次存完，並用 `revision` 做樂觀鎖；有衝突時提示重新載入，不能默默覆蓋。
12. 售價不跟著版本走：售價存在 `menu_prices`（有歷史紀錄）。成本會用現行價格即時計算；**定版時另外寫入 `cost_snapshots`**，內容包含當時用到的價格 id 與逐行明細。
13. 試做項目只能掛在試菜中、待核准或已定版的版本上。評分者可以修改自己的評分，直到該版本定版或停用為止。

---

## 5. 操作紀錄

- 所有業務資料表都要掛上共用的 audit trigger。每次新增、修改、刪除都寫入 `audit_logs`，內容包含操作者、時間、資料表、紀錄 id、修改前後的 jsonb、有變動的欄位。
- `audit_logs` 只能新增。任何角色（包含創辦人）都不能透過 API 修改或刪除。
- **新增資料表時，一定要同時**：放在 `app` schema、啟用 RLS、掛上 audit trigger（0002 的迴圈只會處理當時已存在的表，之後新增的表要自己掛）；如果是版本內容表，還要掛凍結 trigger；並用 RPC 提供存取。
- **新增或修改 RPC 的 migration，結尾一定要重跑 `20260916000006_grants.sql` 的權限區塊**。Supabase 預設會把新函式開放給 `anon`，不重跑就會漏權限。

---

## 6. 技術約定

- 前端：React 19 + TypeScript 7 + Vite 8、React Router 8（`createHashRouter`，靜態託管不需要伺服器改寫網址）、TanStack Query、Tailwind CSS 4、decimal.js、lucide-react。UI 元件自己寫在 `src/components/ui.tsx`，沒有用 shadcn/ui。
- 後端：Supabase（Postgres、Auth、Storage 私有 bucket `photos`）。migrations 放在 `supabase/migrations/`；`npm run db:bundle` 會合併成 `supabase/setup-all.sql`，貼到 SQL Editor 執行。
- **兩種後端，同一套 SQL**（`src/data/backend.ts`）：
  - 有設定 `VITE_SUPABASE_URL`、`VITE_SUPABASE_ANON_KEY` 時使用 Supabase
  - 沒設定時使用**示範模式**：瀏覽器內的 PGlite 執行同一套 migrations 與 RPC，並載入虛構示範資料（`src/data/demoSeed.ts`）
  - `supabase/local/auth_shim.sql` 只給 PGlite 用，模擬 Supabase 的 `auth.uid()`、`anon`、`authenticated`
- 前端計算成本的流程：`get_costing_bundle` 取回版本、巢狀元件與原物料單價 → `src/domain/costing.ts` 計算。定版時由前端算好 `buildSnapshot()` 交給 `transition_version` 寫入。
- 照片：上傳前在瀏覽器壓縮（最長邊 1600px、WebP），並移除 EXIF（包含 GPS）；用 signed URL 讀取。Storage policy 在 `supabase/storage.sql`。
- 時區 `Asia/Taipei`；幣別新台幣
- 目錄分工：
  - `src/domain/`：純計算與狀態機，**不能 import React 或 Supabase**
  - `src/data/`：後端介面、RPC 呼叫、回傳型別、示範資料、基準版匯入
  - `src/pages/`：各功能頁面；`src/components/`：共用元件；`src/app/`：路由外框、登入狀態、格式化
  - `src/i18n/labels.ts`：所有 enum 的中文標籤集中在這裡
  - `db-tests/`：在 PGlite 上跑的資料庫測試；`scripts/`：SQL 合併、基準版菜單轉檔

---

## 7. 測試要求

- **修改 `src/domain/` 時，一定要同時新增或修改 Vitest 測試**，行覆蓋率維持 ≥ 95%。
- **黃金範例**：`src/domain/__fixtures__/golden/` 放手算的完整食譜（原物料 → 元件 → 菜品），逐行比對，比較到小數 10 位必須完全相同（循環小數無法用有限位數表示）。要改期望值，必須先取得使用者同意，並在 commit 說明原因。
- **計算相關的 bug，先補一個會失敗的測試，再修正。**
- **資料庫測試在 PGlite 上跑**（`db-tests/`，不需要 Docker）：每個 RPC 都要測各角色能不能呼叫；凍結 trigger、狀態轉移、audit trigger、唯一約束、照片權限函式也都要測。`db-tests/integration.test.ts` 會比對資料庫的單位表與狀態轉移表是否和 `src/domain` 一致。
- 主要流程要有 Playwright E2E（手機視窗 390×844、zh-TW）：建立菜品並試菜、核准定版、修改已定版食譜、更新原物料價格、內場查看與列印標準卡、測試人員評分。**目前尚未建立**，改畫面流程時先用瀏覽器實際操作驗證，並告訴使用者 E2E 還沒補。
- 說「完成」之前，要先跑 `npm run typecheck` 與 `npm test`（含計算核心與資料庫測試）；有改畫面就在瀏覽器實際操作一次。沒跑的項目要明確告訴使用者。
- **禁止為了讓測試通過而放寬權限檢查、凍結 trigger 或黃金範例的期望值。**

---

## 8. 開發慣例

- 介面文字一律使用繁體中文（台灣用語）；程式識別字、資料表、enum 值用英文。
- commit 訊息用繁體中文。
- **repo 是公開的，repo 內不能出現真實的食譜配方、克重、比例、進價或供應商聯絡資料**；文件舉例時也不要寫真實菜名與份量。seed 資料、測試資料、截圖一律使用虛構資料。使用者已同意 `scripts/import_baseline.py` 裡的原物料名稱與別名清單公開。
- 真實菜單只放在 `private/`（已列入 .gitignore）：`npm run seed:baseline` 讀取上一層資料夾的《餐廳菜單_完整試作與配方表.xlsx》，產生 `private/baseline-seed.json`，再到「更多 → 資料匯出與匯入」匯入。`vite build` 不會打包 `private/`；修改匯入流程後要確認 `dist/` 裡搜尋不到菜名。
- 金鑰只能放 anon key；service role key 不能出現在前端或 repo。
- 手機操作：點擊區至少 44px；數字欄位使用 `inputmode="decimal"`；主要動作放在畫面底部固定列；不依賴 hover。
- Vercel 帳號目前沒有建立專案的權限，部署先不要用 Vercel（見 SPEC §5.5）。
